# Web Framework
# 测试通过版本: fastapi-0.121.1, uvicorn-0.38.0, pydantic-2.10+
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.8.0
pydantic-settings>=2.1.0

# Task Queue
# 测试通过版本: celery-5.5.3, redis-7.0.1
celery>=5.3.0
redis>=5.0.0

# HTTP Client
httpx>=0.25.0
requests>=2.31.0

# Configuration
pyyaml>=6.0

# Web Framework (for frontend)
flask>=3.0.0

# Cloud Storage (for model weight downloading)
boto3>=1.34.0

# AI/ML (Placeholder - 根据实际模型需求调整)
# 测试通过版本: opencv-python-4.12.0.88
# 注意: 使用 opencv-python-headless 避免 GUI 依赖（适合服务器环境）
numpy>=1.26.0
opencv-python-headless>=4.8.0
pillow>=10.0.0

# Medical Imaging
# pydicom: DICOM 医学图像格式解析 (用于处理 X 光片等医学影像)
# pylibjpeg 系列: DICOM JPEG 压缩格式解码 (JPEG Lossless 等)
pydicom>=2.4.0
pylibjpeg>=2.0
pylibjpeg-libjpeg>=2.1

# Deep Learning
# ultralytics: YOLOv8 用于头影测量关键点检测 (pipelines/ceph/modules/point/)
# torch: PyTorch 深度学习框架 (ultralytics 依赖)
# onnxruntime: ONNX 模型推理引擎 (用于髁突分割、下颌骨分割等模块)
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0
onnxruntime>=1.16.0

# Utilities
# 测试通过版本: python-multipart-0.0.20
python-multipart>=0.0.6
python-dotenv>=1.0.0

